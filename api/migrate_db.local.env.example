# Copy this file to  migrate_db.local.env  and paste your two Render
# *External* Database URLs. That real file is gitignored — never commit it.
#
# Render dashboard -> each Postgres -> "Connections" -> External Database URL

OLD_DATABASE_URL=postgresql://user:password@dpg-OLD.oregon-postgres.render.com/olddb
NEW_DATABASE_URL=postgresql://user:password@dpg-NEW.oregon-postgres.render.com/newdb
