# Copy this file to  migrate_db.local.env  and paste your two Database URLs.
# That real file is gitignored — never commit it.
#
# OLD = Render dashboard -> Postgres -> "Connections" -> External Database URL
# NEW = Neon (or other) connection string

OLD_DATABASE_URL=postgresql://user:password@dpg-OLD.oregon-postgres.render.com/olddb
NEW_DATABASE_URL=postgresql://user:password@ep-NEW.aws.neon.tech/neondb?sslmode=require
